"use client";

import { Typography } from "@mui/material";

export default function TestPage() {
  return (
    <div>
      <Typography variant="h4">Test Page</Typography>
      <p>ถ้าเห็นหน้านี้ แสดงว่า Layout + AuthGuardRedirect ทำงานปกติ</p>
    </div>
  );
}
